Запустите MSU-AI-term-project.ipynb для параметризации
  *данных с одного фазового слоя (хранятся в папке "Calibration Example_915")
  *данных с полной структуры (хранятся в папке "full-structure-data")

NB: структура чипов немного отличается. Для корректного фитирования данных с одного фазового слоя необходимо выбрать флаг single_layer = True в начале скрипта, либо поменять его на False для работы с полной структурой.
